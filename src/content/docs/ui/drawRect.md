---
title: function ui.drawRect()
---

```lua
function ui.drawRect(p1, p2, color, rounding, roundingFlags, thickness) end
```

`p1` : vec2  
`p2` : vec2  
`color` : rgbm  
`rounding` : number  
`roundingFlags` : ui.CornerFlags  
`thickness` : number
